# РЕМС-РОЗКЛАД

Перша вебверсія кафедральної системи розкладу.

Файли:
- `index.html` — головна сторінка
- `styles.css` — оформлення
- `data.js` — початкові дані
- `app.js` — логіка системи

Поточна версія зберігає редагування у `localStorage` браузера.
