REMS-РОЗКЛАД v2.1.0 — PERFORMANCE PASS 1

Без зміни структури даних, розкладу чи інтерфейсу:
- локальне збереження великої бази об’єднується протягом 650 мс замість серіалізації через 40 мс;
- save() більше не змушує сторінку синхронно перемальовуватись перед навігацією;
- кілька викликів save() в одному кадрі дають один render;
- навігація скасовує зайвий відкладений render;
- після успішного Firebase-запису локальний стан не проганяється вдруге через migrate/repair/render;
- realtime Firebase для змін з інших клієнтів збережено.

Оригінальна база не змінювалась; це окрема оптимізована копія.
- додано кеш розкладу за групами: календар більше не фільтрує весь schedule для кожного дня/вкладки/бейджа;
- додано кеш зайнятості аудиторій за дата+аудиторія+пара: сітка аудиторій не сканує весь schedule для кожної комірки.
